'use client';

// SendEmail component intentionally disabled.
// The project no longer supports sending emails from the admin UI.
// We keep a small stub export so any imports don't break the build.

import React from 'react';

export default function SendEmail() {
  return null; // UI removed intentionally
}